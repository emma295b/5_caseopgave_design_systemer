---
const { titel, beskrivelse, tid, dato, billede } = Astro.props;
import Button from "./Button.astro";
import "../styles/style.css";
---

<section>
    <div class="textcontent">
         <div class="grid">
        <h3>{titel}</h3>
        <p>{beskrivelse}</p>
            <img src="./icons/klokkeicon.svg" alt="">
            <p>{tid}</p>
        </div>
        <div class="grid">
            <img class="billede" src="./pictures/workshop_2_img.svg" alt="">
            <img src="./icons/datoicon.svg" alt="">
            <p>{dato}</p>
        </div>
        <Button variant="secondary">
            Læs mere her
        </Button>
    </div>
</section>


<style>
    section{
    display: grid;
    grid-template-columns: 1fr 1fr;
    display: inline-flex;
    width: 100%;
    height: 100%;
    padding-left: 30px;
    justify-content: center;
    align-items: center;
    gap: 36px;
    background-color: var(--bg-color-primary);
    color: var(--text-color-primary);
    text-align: left;
    }

    .billede {
        flex-direction: column;
        justify-content: flex-end;
        align-items: flex-end;
        gap: 40px;
        display: inline-flex;

    }

    h3 {
    width: 100%; 
    height: 100%; 
    flex-direction: column; 
    justify-content: center; 
    align-items: center; 
    gap: 32px; 
    display: inline-flex"
    }

    .textcontent{
        padding: 60px;
    }
	
</style>
